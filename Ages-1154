#include<stdio.h>

int main()
{
    int n ;
    double ans, sum=0, count=0;
    while(1)
    {

        scanf("%d",&n);

        if(n<0) break;

        if(0<=n)
        {
            count ++;
            sum += n;
        }
    }
    ans = sum/count;
    printf("%0.2lf\n",ans);
}

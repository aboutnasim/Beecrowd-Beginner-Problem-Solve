#include<stdio.h>

int main()
{
    int x,z, count=0, sum=0;
    scanf("%d %d",&x,&z);

    while(z<=x) scanf("%d",&z);

    for(int i = x; ; i++)
    {
        count++;
        sum +=i;

        if(sum>z) break;
    }

    printf("%d\n",count);
}

#include<stdio.h>
int main()
{
    float N, x, y, z, i, sum, avg;

    scanf("%f",&N);

    for(i=1; i<=N; i++)
    {
        scanf("%f %f %f",&x, &y, &z);

        sum=x*2+y*3+z*5;
        avg=sum/(2+3+5);
        printf("%0.1f\n",avg);
    }
    return 0;
}

#include<stdio.h>

int main()
{
    int i;
    double n, a[100];

    for(i=0; i<100; i++)
    {
        scanf("%lf",&n);
        if(n>10) continue;
        a[i]=n;

        printf("A[%d] = %0.1lf\n",i,a[i]);
    }
}

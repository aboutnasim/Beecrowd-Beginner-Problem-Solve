#include<stdio.h>
int main()
{
    int p,j1,j2,r,a,ans;
    int sum =0;
    scanf("%d %d %d %d %d",&p, &j1, &j2, &r, &a);
    sum = j1+j2;

    if ((p==1 && sum%2==0) || (p==0 && sum%2!=0)) ans=1;
    else ans=2;

    if((r==1 && a==0) || (r==0 && a==1)) ans=1;
    else if(r==1 && a==1) win=2;


    printf("Jogador %d ganha!\n",ans);
}
